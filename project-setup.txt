open a terminal windows (start running mamp)

1. any change you make to your css, make it in the root /css folder. You'll see .scss (SASS) files.
2. After you make your change, type npm run build. 
3. The resulting compilied css will be in dist/css/style.min.css 